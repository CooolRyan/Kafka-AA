# 배포 대상 (복사 후 local-server.env 로 저장하고 값 조정)
# 이 호스트는 NCP PEM이 아니라 ubuntu/ubuntu 비밀번호 SSH가 전제입니다. deploy-jar.sh 는 에이전트 키 없이 실행 시 비밀번호 입력.
DEPLOY_HOST=192.168.160.147
DEPLOY_USER=ubuntu
DEPLOY_PORT=22
# 원격에 올릴 디렉터리 (없으면 mkdir -p)
REMOTE_DIR=/home/ubuntu/apps/kafka-active
